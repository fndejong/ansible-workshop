---

- authorized_key: #Add an authorized key
    user: ansible # De gebruiker om de authorized key voor toe te voegen
    state: present # Voeg de key toe (gebruik 'absent' om de key te verwijderen)
    key: "{{ lookup('file', lookup('env','HOME') + '/.ssh/id_rsa.pub') }}" # Haal de public key op van de huidige gebruiker
  become: true

...
